import { FilterByPipe } from './filter-by.pipe';

describe('FilterBySearchCriterionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPipe();
    expect(pipe).toBeTruthy();
  });
});
