import { ExpireWarnDirective } from './expire-warn.directive';

describe('ExpireWarnDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpireWarnDirective();
    expect(directive).toBeTruthy();
  });
});
